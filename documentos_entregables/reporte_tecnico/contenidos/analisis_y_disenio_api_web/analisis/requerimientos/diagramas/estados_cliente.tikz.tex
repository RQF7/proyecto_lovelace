%
% Diagrama de estados del cliente,
% Análisis y diseño de aplicación web.
% Proyecto Lovelace.
%
% TODO: Definir hipervínculos a casos de uso en las etiquetas de los
% cambios de estado.
%

\begin{tikzpicture}[
  estado/.style = {
    rectangle,
    draw = black,
    thin,
    inner sep = 4mm,
    text width = 25mm,
    align = center,
    minimum height = 1.5cm},
  estado_inicial/.style = {
    circle,
    minimum size = 15pt,
    fill = black,
  },
  etiqueta_transicion/.style = {
    align = center
  }]

  % Estados
  \node[estado_inicial]
    (estado_inicial) {};
  \node[estado]
    (no_verificado)
    [right = 1.4in of estado_inicial]
    {\small No verificado};
  \node[estado]
    (verificado)
    [right = 1.4in of no_verificado]
    {\small Verificado};
  \node[estado]
    (rechazado)
    [above = 0.6in of verificado]
    {\small Rechazado};
  \node[estado]
    (aprobado)
    [right = 1.4in of verificado]
    {\small Aprobado};
  \node[estado]
    (en_cambio_de_llaves)
    [above = 0.6in of aprobado]
    {\small En cambio de llaves};
  \node[estado]
    (en_lista_negra)
    [below = 0.6in of aprobado]
    {\small En lista negra};

  % Transiciones
  \draw[-Stealth]
    (estado_inicial.east)
    --
    node[
      etiqueta_transicion,
      text width = 1.2in,
      below]
      {\small \textsc{CUAPI03 Registrar usuario} - enviar datos}
    (no_verificado.west);
  \draw[-Stealth]
    (no_verificado.east)
    --
    node[
      etiqueta_transicion,
      text width = 1.2in,
      below]
      {\small \textsc{CUAPI03 Registrar usuario} - verificar correo}
    (verificado.west);
  \draw[-Stealth]
    (verificado.north)
    --
    node[
      etiqueta_transicion,
      text width = 1.2in,
      left]
      {\small \textsc{CUAPI04 Rechazar usuario}}
    (rechazado.south);
  \draw[-Stealth]
    (verificado.east)
    --
    node[
      etiqueta_transicion,
      text width = 1.2in,
      below]
      {\small \textsc{CUAPI05 Aprobar usuario}}
    (aprobado.west);
  \draw[-Stealth]
    ($(aprobado.north) - (0.2in, 0)$)
    --
    ($(en_cambio_de_llaves.south) - (0.2in, 0)$);
  \draw[-Stealth]
    ($(en_cambio_de_llaves.south) + (0.2in, 0)$)
    --
    ($(aprobado.north) + (0.2in, 0)$);
  \draw[-Stealth]
    ($(aprobado.south) - (0.2in, 0)$)
    --
    ($(en_lista_negra.north) - (0.2in, 0)$);
  \draw[-Stealth]
    ($(en_lista_negra.north) + (0.2in, 0)$)
    --
    ($(aprobado.south) + (0.2in, 0)$);
  \draw[-Stealth]
    ($(en_cambio_de_llaves.east) + (0, -0.1in)$)
    --
    ($(en_cambio_de_llaves.east) + (0.4in, -0.1in)$)
    |-
    ($(en_lista_negra.east) + (0, 0.1in)$);
  \draw[-Stealth]
    ($(en_lista_negra.east) + (0, -0.1in)$)
    --
    ($(en_lista_negra.east) + (0.6in, -0.1in)$)
    |-
    ($(en_cambio_de_llaves.east) + (0, 0.1in)$);

\end{tikzpicture}
