/*
 * Controlador de página de documentación.
 * Aplicación web de sistema tokenizador.
 * Proyecto Lovelace.
 */

sistemaTokenizador.controller('controladorDocumentacion', [
  '$scope',
  function (
    $scope
  )
  {
    $scope.cambiarTitulo("Documentación", 2);
  }
]);
